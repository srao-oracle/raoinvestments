# Copy to .env.local and fill in. NEVER commit .env.local.

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=https://sgzobeuxwgjwoifvbtmh.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-publishable-anon-key
SUPABASE_SERVICE_ROLE_KEY=replace-with-service-role-key   # server-only, never NEXT_PUBLIC
SUPABASE_DB_PASSWORD=replace-with-db-password
SUPABASE_PROJECT_REF=sgzobeuxwgjwoifvbtmh

# --- AI ---
ANTHROPIC_API_KEY=sk-ant-replace-me

# --- Market / macro data ---
MASSIVE_API_KEY=replace-with-massive-polygon-key
FRED_API_KEY=replace-with-fred-key

# --- App ---
NEXT_PUBLIC_SITE_URL=https://raoinvestments.app
ALLOWLIST_EMAILS=you@example.com,partner@example.com

# --- Cron / infra ---
CRON_SECRET=replace-with-long-random-string
KILL_SWITCH=false

# --- Deploy tooling tokens (CI / local deploy only; never bundled into the app) ---
FLY_API_TOKEN=replace-with-fly-token
VERCEL_API_TOKEN=replace-with-vercel-token
